이 코드는 main에서 입력된 문자를 빈번도에 따라 호프만 코드를 할당해줍니다.
그 후, 어떠한 문자열에 대하여, 문자열을 호프만 코드로 바꿔주고, 
그 호프만 코드가 원래 문자열이 맞는지 다시 출력하여 확인해줍니다. 

만약 빈번도와 문자를 바꾸고 싶다면, main에서 순서에 맞추어 data와 freq를 다시 지정해주면 됩니다.
또한, 다양한 문자열의 코드를 알고 싶다면, 문자열 변수 str에 새로운 문자열을 할당해주면 됩니다.
그러면 그 문자열에 대한 호프만 코드를 확인할 수 있습니다.