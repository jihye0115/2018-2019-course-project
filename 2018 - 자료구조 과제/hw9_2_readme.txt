이 코드는 0에서 10000 사이에 해당하는 1000개의 랜덤 변수를 생성하여 정렬한다.
만약 값의 범위를 바꾸고 싶다면 data_maxval을 조정해주면 된다. 
예를 들어, 500까지의 값을 원한다면 data_maxval=500;이다.

만약 값의 개수를 바꾸고 싶다면 input_size를 조정해주면 된다.
예를 들어, 100개의 랜덤 변수를 생성하고 싶다면 input_size=100;이다.

이 코드는 binary search tree를 이용해 값을 정렬한다.