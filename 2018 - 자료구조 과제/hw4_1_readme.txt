이 코드는 스택에 데이터를 넣거나 스택에서 데이터를 삭제할 수 있습니다.

스택을 리스트로 구현하였으며, 가장 최근에 삽입된 노드의 주소는 phead에 저장되어 있습니다.
추가로 데이터를 저장하고 싶다면 push(phead, x); (여기서 x는 저장하고 싶은 값) 함수를 main에서 실행하고,
데이터를 삭제하고 싶으면 pop(phead);를 메인에서 호출하면 됩니다.

printf("%d\n",pop(phead));를 통해 가장 최근의 데이터를 삭제하며 출력할 수 있습니다.